# Trusted Host Daemon Perspective

The Go Trusted Host Daemon operates as a desktop sidecar service providing Tailscale WireGuard mesh networking (`tsnet`), JWT authentication middleware, location WebSocket telemetry streaming, Markdown vault watching, and LWW conflict resolution.
